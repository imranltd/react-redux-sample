import { getChatLog, getMemberList } from './service';

it('returns getChatLog object', () => {
  it(expect(typeof getChatLog()).toEqual('object'));
});

it('returns getChatLog object', () => {
  it(expect(typeof getMemberList()).toEqual('object'));
});